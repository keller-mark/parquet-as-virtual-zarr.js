// TODO: implement a ArrowAsAnnDataFrameStore class (must implement zarrita's AsyncReadable interface)
//
// The constructor should accept an array/buffer of arrow IPC bytes.
// The zarr store should then expose the table via the same anndata dataframe zarr-based interface as the ParquetAsAnnDataFrameStore.

// To test this, use the tableToIPC and tableFromIPC functions from Flechette in unit tests
